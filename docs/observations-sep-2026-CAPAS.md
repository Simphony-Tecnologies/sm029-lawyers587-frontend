# Analisis de capa — observations-sep-2026.md (confirmado contra codigo)

Metodo: un agente por item leyo el codigo responsable y confirmo la capa; pasada adversarial retando cada afirmacion de trabajo frontend. Evidencia file:line por item.

## Matriz ID -> capa

| ID | Capa | ¿FE? | Esfuerzo FE | Decision producto |
|----|------|------|-------------|-------------------|
| A1 | FE+BE | si | medio | si |
| A2 | FE+BE | si | bajo | si |
| A3 | FE+BE | si | medio | si |
| B1 | FE+BE | si | bajo | — |
| B2 | FE+BE | si | bajo | — |
| B3 | FE | si | bajo | — |
| B4 | FE+BE | si | medio | si |
| B5 | FE | si | medio | menor |
| C1 | FE+BE | si | bajo | si |
| C2 | PRODUCTO | si | bajo | si |
| C3 | FE+BE | si | bajo | si |
| D1 | FE+BE | si | medio | si |
| D2 | BE | no | ninguno | — |
| D3 | FE+BE | si | bajo | si |
| E1 | FE | si | bajo | si |
| F1 | FE+BE | no | alto | si |
| G1 | FE | si | bajo | — |

**Resumen:** 15 items con trabajo frontend accionable · 2 no-frontend (D2 = BE puro · F1 = FE bloqueado por falta de endpoints backend) · 11 items tienen además una decisión de producto que fija el fix definitivo.

---

## LO QUE ES FRONTEND (accionable en este repo)

### A1 — capa FE+BE · esfuerzo FE medio · confianza high
- **Causa raiz:** `role` es atributo de primera clase; admins y lawyers comparten tabla/endpoint `/lawyers`. El listado se pide SIN filtro de rol → los admins salen como filas. `LawyerFormModal` no tiene conciencia de rol → renderiza campos de capacidad siempre. Create hardcodea `role_id:2` y descarta `/roles` → no existe ruta de alta de admin.
- **Frontend (fix aqui):** (1) Ocultar y NO enviar capacidad cuando `role!='lawyer'`: `LawyerFormModal.tsx:380-436` (Areas + No. Leads) y `:511-518` (ActivityBlock) gate con nueva prop `role`; `LawyerFormInitialData:48-69` add `role`; `LawyerManagement.tsx:939-984` pasar `dataIndex.role?.name`; saltar el diff de services en `handleUpdateLawyer:537-594` cuando `role!='lawyer'`. (2) Filtrar admins del listado: `LawyerManagement.tsx:128` por `role.name==='lawyer'` o reusar `api.lawyers.list({role_id:2})` (ya usado en `LeadManagement.tsx:495`). Añadir un selector de rol al form de create es FE pero bloqueado por contrato BE.
- **Necesita backend:** endpoint de alta de admin + enforcement de permisos. `POST /lawyers` hoy recibe `role_id:2` hardcodeado; si acepta `role_id=1` y enforce "solo un admin global crea admin" no es verificable/enforceable en FE (el middleware solo gatea rutas, no esta mutacion).
- **Decision de producto:** ¿un admin puede además recibir leads (rol dual) o son excluyentes? Hoy el modelo implica excluyente (un solo `role_id`).
- **Evidencia:** `lawyerData.types.ts:13-16` (role atributo); `LawyerManagement.tsx:118-128` (list sin filtro de rol), `:458` (role_id:2 hardcoded), `:614-623` (/roles fetched y descartado); `LawyerFormModal.tsx:380-436,511-518`; `LeadManagement.tsx:495`.

### A2 — capa FE+BE · esfuerzo FE bajo · confianza high
- **Causa raiz:** El handler de borrado muestra un string estatico e ignora code/messages que el servicio YA parsea de la respuesta (surfacing arreglable en FE hoy). Pero el backend probablemente falla por FK o regla de permiso sobre rol admin sin exponer statusCode/message diferenciado, por lo que la UX de tres casos (403/409/500) requiere tambien cambio de contrato en el servidor. Doble causa -> FE+BE.
- **Frontend (fix aqui):** Confirmado que HAY trabajo FE accionable HOY, pero mas acotado de lo que afirmaba el primer analista. En LawyerManagement.tsx:369-370 (DeleteLawyer) y :387-388 (ConfirmMultipleDelete) el handler muestra el string estatico 'Error to delete lawyer' y descarta dataDelete.messages y dataDelete.code, que el servicio database.ts:838-842 YA parsea (code=data.statusCode??response.status, messages=data.message). FE-only HOY: reemplazar el string estatico por `toast.error(dataDelete.messages ?? 'Error to delete lawyer')` y opcionalmente anexar el code, para superficiar el error REAL que el backend ya devuelva. El patron ya existe in-file en :532-533 (updateData.code===401 -> toast.error(updateData.messages)). LO QUE NO ES FE-only: el mapeo semantico de tres casos (403 sin permiso / 409 dependencias / 500 fallo interno) solo funciona si el backend emite esos statusCode+message distintos; hoy no lo hace (ver beWork), asi que sin cambio de contrato el toast solo puede mostrar lo generico que llegue. El guard de auto-eliminacion en FE (comparar dataIndex.id con usuario en sesion) es solo defensa superficial; la enforcement real es server-side.
- **Necesita backend:** DELETE /lawyers/:id debe (1) devolver statusCode y message diferenciables: 403 sin permiso o rol admin protegido, 409 con detalle cuando hay integridad referencial (FK desde leads/activity_log/notifications/firm_admins), 500 solo en fallo real; (2) bloquear el borrado con dependencias en vez de cascada, ofreciendo desactivacion (ligado a A3); (3) impedir auto-eliminacion del usuario en sesion server-side. Hoy 'pending setup' se borra por no tener dependencias; el admin falla por FK o permiso pero devuelve un error no diferenciado, por lo que el front no puede discriminar los tres casos aunque el service ya extraiga code/messages. Sin este cambio de contrato, el mapeo semantico FE es imposible.
- **Decision de producto:** Item 6 del doc (ligado a A2/A3): definir si el borrado de un abogado con historico debe bloquearse (409) o anonimizarse/soft-delete. Condiciona la respuesta del backend y el copy del toast.
- **Evidencia:** src/app/(dashboard)/lawyer-management/LawyerManagement.tsx:369-370 (toast estatico DeleteLawyer), :387-388 (borrado masivo estatico), :532-533 (patron code/messages ya usado con code===401); src/services/database.ts:827-852 (DeleteLawyer devuelve code=data.statusCode??response.status y messages=data.message, ya parseados); docs/observations-sep-2026.md A2 (diagnostico FK/permiso, alcance pide distinguir 403/409/500). Todos los file:line citados por el primer analista existen y sostienen la causa raiz.

### A3 — capa FE+BE · esfuerzo FE medio · confianza high
- **Causa raiz:** Deactivate es un soft-flag (`is_active=false`) y su `comment` (reason) SÍ se captura y envía al backend; la "pérdida" percibida es un gap de UI — las cuentas desactivadas renderizan bajo `unassignable`/`pending` (no existe status `Inactive`) y el renderer de `status_change` omite `ev.comment`. Delete es `DELETE /lawyers/{id}` sin reason ni copy de consecuencia; su efecto real en BD no es observable desde FE.
- **Frontend (fix aqui):** (1) Segmento `Inactive`: extender `LawyerStatus` + styleMap en `LawyerStatusPill.tsx:4-8,15-36`; separar desactivados (`!isActive`) en status `'inactive'` en `LawyerManagement.tsx:844-848` (hoy plegado en unassignable/pending); `FilterButton` Inactive en `:1301-1306` alimentado por `statsServer.inactive` (ya disponible, hoy mal-cableado en el KPI unassignable `:893-895`). (2) Mostrar el reason en el activity log: `IdLawyer.tsx:299-308` muestra solo `from→to` y descarta `ev.comment` — añadirlo (el campo ya fluye en `AuditEvent`, `api.types.ts:168`). (3) Declarar consecuencias: copy "reversible, no leads" en el modal Deactivate (`:1084-1126`), "irreversible" en Delete (`:1044-1072`).
- **Necesita backend:** confirmar qué persiste `DELETE /lawyers/{id}` (físico vs soft); confirmar que el `comment` de deactivate se guarda en `audit_log` y lo devuelve `/lawyers/{id}/history` (FE ya lo envía: `PATCH status {is_active,comment}`); si Delete debe capturar reason, añadir param; confirmar que el list endpoint devuelve inactivos.
- **Decision de producto:** ¿`Inactive` es un segmento distinto de `Unassignable` (activo sin capacidad) y `Pending` (nunca onboardeado)? Política de Delete (gate "sin histórico"/irreversible, ¿requiere reason?).
- **Evidencia:** `LawyerManagement.tsx:676-699` (deactivate envía is_active+comment), `:844-848`, `:893-895`, `:1044-1072`; `LawyerStatusPill.tsx:4-8`; `IdLawyer.tsx:299-308`; `database.ts:827-852`; `api.types.ts:342,347-350`.

### B1 — capa FE+BE · esfuerzo FE bajo · confianza high
- **Causa raiz:** El abogado no está en el array de /lawyers (scoping por firma o usuario huérfano sin perfil) → como el buscador filtra client-side sobre ese array, nunca aparece. La contradicción 22 vs 0 es porque el header lee /lawyers/stats y la tabla lee /lawyers: dos endpoints con predicados distintos mezclados en el FE.
- **Frontend (fix aqui):** Confirmado FE-only y de bajo esfuerzo: unificar la fuente del contador. Hoy el header (LawyerManagement.tsx:1152/1180/1279) muestra `kpis.total = statsServer?.total ?? localTotal` (:889), donde statsServer viene de /lawyers/stats (database.ts:1135-1136), mientras el pie de la tabla "Showing X of Y lawyers" (DataTable.tsx:352-361) cuenta `sortedData.length` derivado de `filteredRows` (data prop en :1352), que sale de /lawyers. Cuando ambos endpoints tienen scope distinto, header=22 y pie=0. Fix FE puro: derivar el número del header del mismo `lawyerRows`/`localTotal`, o mostrar ambos rotulados. Ambos valores (statsServer y localTotal) ya están en estado del componente, así que decidir cuál pintar es presentación pura, sin cambio de contrato/payload backend. CAVEAT: este cambio es cosmético y puede ENMASCARAR el bug real (forzar 22=0 oculta la discrepancia); no recupera al abogado ausente. La hipótesis "buscador case-sensitive" NO aplica: el filtro ya es case-insensitive (:913-922, toLowerCase().includes). Nada que arreglar en el buscador FE.
- **Necesita backend:** Causa raíz del bug reportado (abogado no aparece en lista NI en buscador) es backend: GET /lawyers no devuelve ese registro. Verificar (1) integridad: existe fila en tabla lawyers o solo usuario auth huérfano sin perfil; (2) scoping por firm_id/law_firm — Lawyer Demo (587lawyers.com) vs admin (catmarketing.ca), la query filtra por firma; (3) unificar el predicado entre GET /lawyers (lista) y GET /lawyers/stats (total agregado) para que no reporten poblaciones distintas. El FE no puede recuperar una fila que el backend no envía: getData (database.ts:518-537) y fetchData (:118-120) no mandan query de búsqueda ni filtro persistente.
- **Evidencia:** LawyerManagement.tsx:118-120 fetch /lawyers sin params; database.ts:518-537 getData sin query de búsqueda; LawyerManagement.tsx:913-922 búsqueda ya case-insensitive; LawyerManagement.tsx:889 kpis.total = statsServer?.total ?? localTotal; LawyerManagement.tsx:988 setStatsServer desde api.lawyers.stats; database.ts:1135-1136 stats->/lawyers/stats; DataTable.tsx:352-361 pie "Showing X of Y" usa sortedData.length; LawyerManagement.tsx:1351-1352 data={filteredRows} (:908-925)

### B2 — capa FE+BE · esfuerzo FE bajo · confianza high
- **Causa raiz:** El frontend renderiza un toast hardcodeado unico que conflaciona 'email existe' con 'error al crear', ignorando creating.code y creating.data que ya estan disponibles. La diferenciacion util de mensajes depende de que el backend emita status/error_code distintos (contrato BE), y la queja 'el email no esta repetido' apunta a dedup/scoping en persistencia backend. Por eso hay una parte FE (dejar de hardcodear, surface del body) y una parte BE (emitir codigos + arreglar dedup).
- **Frontend (fix aqui):** Frontend real y accionable HOY en src/app/(dashboard)/lawyer-management/LawyerManagement.tsx:466-470. Reemplazar el toast hardcodeado unico ('Email exists or error to create lawyer') por mensajes diferenciados. CORRECCION al primer analista: NO hace falta tocar database.ts para 'poblar messages' — CreateLawyer ya retorna el body completo del backend en creating.data (database.ts:617) y el status HTTP en creating.code (database.ts:616), asi que el componente ya puede leer creating.data?.message / creating.data?.error_code / creating.code HOY. Fix FE-only con fallback graceful: 1) si creating.data?.message existe, mostrarlo; 2) si no, ramificar por creating.code (409 -> 'This email is already registered'; 400 -> 'Validation error'; 403 -> 'You do not have permission'; 5xx -> 'Could not create lawyer, try again'). Ese fallback mejora UX sin depender del backend. LIMITE: la diferenciacion REAL de causas (email duplicado vs validacion vs permisos) solo funciona si el backend emite status/error_code distintos; si el backend colapsa todo en un 400/500 generico, el branching FE no distingue nada -> esa parte depende de contrato BE.
- **Necesita backend:** 1) POST /lawyers debe responder con status HTTP semantico (409 email duplicado, 400 validacion con campo, 403 permisos, 500 interno) y/o un error_code en el body (EMAIL_ALREADY_EXISTS, VALIDATION_FAILED, PERMISSION_DENIED). Sin esto el frontend no puede distinguir causas aunque ramifique. 2) Investigar la logica de dedup: el cliente afirma que el correo NO esta repetido pero recibe colision -> probablemente el unique constraint choca contra registros soft-deleted/desactivados o de otra firma (scoping). Persistencia/reglas de negocio, fuera de este repo.
- **Evidencia:** src/app/(dashboard)/lawyer-management/LawyerManagement.tsx:466 (if !creating.success) y :467 (toast.error hardcodeado 'Email exists or error to create lawyer'); src/services/database.ts:616 (code: response.status disponible) y :617 (data: body completo del backend ya disponible, contradice la afirmacion de que FE no tiene acceso al motivo real); src/types/Response/response.interface.ts:5 (messages?: any existe pero es redundante porque data ya trae el body); database.ts:607 POST a /lawyers (backend remoto, fuera del repo).

### B3 — capa FE · esfuerzo FE bajo · confianza high
- **Causa raiz:** El autofill de navegador se controla SOLO con atributos HTML client-side (autocomplete/name). Ningún input del modal los declara, así que Chrome interpreta el primer text input (Name of Law Firm) como campo de identidad y le inyecta el email guardado del admin, y el input type=password recibe la credencial almacenada de la sesión. El estado del form ya se resetea a vacío al abrir; el navegador repinta valores tras el render. No hay dependencia de payload/contrato de backend: el password solo se envía como payload.password en mode='new' y el backend no emite nada que afecte la decisión de autofill.
- **Frontend (fix aqui):** Mitigar el autofill del gestor de contraseñas de Chrome en LawyerFormModal (src/components/ui/organisms/LawyerFormModal/LawyerFormModal.tsx), 100% client-side HTML: 1) Password modo 'new' (Input type='password', lineas 501-507): añadir autoComplete='new-password'. 2) 'Name of Law Firm' (Input type='text', lineas 460-466): autoComplete='off' + name no semántico (p.ej. name='lm-firm-xxxx') para que Chrome deje de tratarlo como campo de identidad y lo rellene con info@catmarketing.ca. 3) Email/Phone (lineas 440-455): autoComplete='off'/'email'/'tel-national' según se prefiera. 4) Opcional robusto: honeypot de inputs username+password ocultos al inicio del DialogPanel, o técnica readOnly liberado onFocus. El componente Input hace spread {...rest} (linea 645) por lo que autoComplete/name pasan sin tocar el wrapper. El reset del useEffect (linea 177, password='') YA existe y NO es bug. Fix en un único archivo.
- **Evidencia:** LawyerFormModal.tsx:459-467 (Input 'Name of Law Firm' sin autoComplete/name), :500-508 (Input password 'new' sin autoComplete='new-password'), :439-457 (email/phone sin autoComplete), :633-647 (Input spread {...rest} en :645 permite pasar autoComplete/name), :164-198 (useEffect resetea todo, password='' en :177 — no es bug); grep autoComplete/autocomplete = 0 ocurrencias en el archivo. Montaje: LawyerManagement.tsx:1035-1042 (LawyerFormModal mode='new', onSubmit=handleCreateLawyer).

### B4 — capa FE+BE · esfuerzo FE medio · confianza high
- **Causa raiz:** 'Name of Law Firm' es texto libre que viaja como cadena `law_firm` a POST/PUT /lawyers; la asociación a la entidad firma la resuelve el backend por auto-vinculación de ortografía canónica. No hay firm_id ni catálogo en el front, así que variaciones de escritura pueden crear/duplicar firmas de forma opaca. El fix real depende de un cambio de contrato de payload (firm_id) + endpoint de catálogo, no solo de UI.
- **Frontend (fix aqui):** DEFINITIVO (bloqueado hoy): reemplazar el free-text por un selector/combobox contra catálogo de firmas en LawyerFormModal.tsx:459-467 y mapear firm_id en el payload en LawyerManagement.tsx:460 y :526 (hoy `law_firm: payload.name_of_law_firm`). Esto NO es fix FE puro: no existe GET de catálogo en database.ts (firms.* solo me/listLawyers/addLawyer/setAdmin/updateSettings/leads/merge, 1315-1363) ni firm_id en el contrato POST/PUT /lawyers (database.ts:607-611). MITIGACIÓN FE-only disponible HOY (parcial, no cierra el root cause): convertir el Input en autocomplete/`<datalist>` sembrado con los valores distintos de `law_firm` ya cargados en la lista de lawyers, para reducir la deriva ortográfica sin endpoint nuevo. La mitigación NO genera firm_id ni evita el auto-vínculo por ortografía del backend.
- **Necesita backend:** 1) Exponer GET catálogo de firmas (id, nombre, estado) para poblar el selector — no existe (parte de F1). 2) Aceptar firm_id en POST/PUT /lawyers en vez de/además de la cadena law_firm. 3) Definir el contrato de creación/dedup: hoy el backend auto-vincula por ortografía canónica (api.types.ts:592) de forma opaca al operador. Todo en el backend staging remoto.
- **Decision de producto:** ¿Escribir un nombre debe CREAR firma nueva o solo SELECCIONAR de catálogo con creación explícita separada? ¿Quién puede crear firmas (super admin / firm admin / ambos)? (Q de la fila F1). Bloquea el diseño del control B4 y precede a cualquier trabajo FE definitivo.
- **Evidencia:** LawyerFormModal.tsx:459-467 (Input texto libre law firm); LawyerManagement.tsx:460,526 (law_firm: payload.name_of_law_firm en create y update); database.ts:602-618 (CreateLawyer POST /lawyers con sendData string); api.types.ts:592 ('ortografía canónica si se auto-vinculó'); database.ts:1315-1363 (firms.* SIN GET de catálogo/creación de firmas).

### B5 — capa FE · esfuerzo FE medio · confianza high
- **Causa raiz:** el chevron es un `<svg>` hermano del `<select appearance-none>` dentro de un flex; el select solo cubre el ancho del texto, dejando el icono y el padding fuera de su hit-area. El `pageSize` vive en `useState` local sin `localStorage` → siempre arranca en 10.
- **Frontend (fix aqui):** 100% FE, sin backend. (1) Chevron: reestructurar `RowsPerPageSelect.tsx:23-37` — wrapper `relative`, `<select>` que llene la caja (`absolute inset-0 w-full h-full` o `flex-1`), chevron como overlay con `pointer-events-none` para que el click pase al select. (2) Persistencia: `DataTable.tsx:85-87` — `pageSize` a `localStorage` (key por tabla vía prop `storageKey`/`tableId`), hidratar el estado inicial. Aplica a TODAS las tablas del panel vía el componente compartido.
- **Necesita backend:** ninguno (localStorage resuelve el requisito "session/local").
- **Decision de producto (menor):** persistencia por-navegador (localStorage, sin BE) vs cross-device por usuario (solo esta última necesitaría endpoint de user-preferences). El scope pide "session/local" → FE puro por defecto.
- **Evidencia:** `RowsPerPageSelect.tsx:23-37`; `DataTable.tsx:85-87`; grep confirma cero uso de localStorage para page/rows.

### C1 — capa FE+BE · esfuerzo FE bajo · confianza high
- **Causa raiz:** El mismo status key 'LOST' (enviado por backend) se rotula distinto en cada punto porque no hay un unico punto de verdad de labels: leadStatusMeta.ts pretende ser catalogo pero StatusPill.tsx, IdLawyer.tsx y SortableTable.tsx mantienen mapas propios y LeadInfoModal.tsx hardcodea copy 'lost' en banner/boton. Resultado: 'Sent back' / 'Send Back' / 'lost' / 'Mark as Lost' para un solo estado. Ademas existe una clave SEND_BACK huerfana (usada en reglas pero no en el catalogo) que delata ambiguedad del enum server-side.
- **Frontend (fix aqui):** SI hay trabajo frontend real y accionable HOY, sin cambio de contrato backend: los labels son strings de display hardcodeados en 4 mapas del front y el mismo status key 'LOST' (que el backend ya envia) se rotula distinto en cada punto. Consolidacion mecanica FE (100% frontend, verificada): (a) src/components/organisms/SortableTable.tsx:301-302 rendea LOST como 'Send Back' (casing distinto al badge 'Sent back') -> leer de getLeadStatusMeta().label. (b) src/components/ui/atoms/StatusPill/StatusPill.tsx:53 (LABELS.lost) e src/app/(dashboard)/lawyer-management/[id]/IdLawyer.tsx:127-128 repiten 'Sent back' en mapas propios -> deben leer del catalogo unico leadStatusMeta.ts. (c) src/components/ui/organisms/LeadInfoModal/LeadInfoModal.tsx:755 banner 'Once marked as lost...' y :971 boton 'Mark as Lost' usan copy 'lost' mientras el badge dice 'Sent back'. Esta parte (estandarizar todo al string canonico actual 'Sent back' que ya usa leadStatusMeta.ts) NO esta bloqueada y se puede hacer hoy; SOLO el texto final definitivo (si debe decir 'Sent back' vs 'Lost' vs ambos) depende del product decision. CORRECCION al analista previo: el item (d) IdLawyer.tsx:261 'Unassigned' NO es un 5to nombre del mismo estado: es el label del ActionType 'unassign' del activity log (una accion sobre la asignacion), concepto distinto del status LOST; forzarlo a 'Sent back' seria incorrecto, por lo que se excluye del dedup de labels de status.
- **Necesita backend:** Confirmar el enum REAL de estados de lead en BD y si LOST y SEND_BACK son un solo estado o dos. Evidencia dura de ambiguedad server-side: el front define la clave SEND_BACK en REASON_REQUIRED_STATUSES (leadStatusMeta.ts:156), DESTRUCTIVE_STATUSES (leadStatusMeta.ts:162) e IdLawyer.tsx:128, PERO SEND_BACK NO existe ni en el type LeadStatusKey (leadStatusMeta.ts:1-12) ni en LEAD_STATUS_META; getLeadStatusMeta() haria fallback a 'Disabled' si el backend emitiera 'SEND_BACK'. Hay que confirmar que valor emite realmente el backend (hoy los dropdowns mandan value 'LOST' para la accion 'Send back'). Si producto decide separar 'Send back' (devolver al pool) de 'Lost' (descartar), es cambio de enum + reglas de transicion + nuevo tipo de evento en el activity log (BE). Mientras el backend emita un unico 'LOST', el fix de labels es 100% FE.
- **Decision de producto:** Bloqueante SOLO para el texto canonico final, no para el dedup mecanico: son 'Send Back' (abogado/admin devuelve el lead al pool) y 'Mark as Lost' (lead descartado del negocio) el mismo evento o dos distintos? Hoy comparten boton/estado LOST. La etiqueta canonica a fijar en el catalogo FE depende de esta respuesta.
- **Evidencia:** Badge/pill 'Sent back': src/components/ui/organisms/LeadInfoModal/leadStatusMeta.ts:79-80, src/components/ui/atoms/StatusPill/StatusPill.tsx:53, src/app/(dashboard)/lawyer-management/[id]/IdLawyer.tsx:127-128. Fila tabla 'Send Back': src/components/organisms/SortableTable.tsx:301-302. Banner 'Once marked as lost': src/components/ui/organisms/LeadInfoModal/LeadInfoModal.tsx:755. Boton 'Mark as Lost': src/components/ui/organisms/LeadInfoModal/LeadInfoModal.tsx:971. Dropdown 'Send back'/'Send Back'->value LOST: src/app/(dashboard)/lead-management/LeadManagement.tsx:86,99,106,120. Clave SEND_BACK huerfana (no en LeadStatusKey/LEAD_STATUS_META): leadStatusMeta.ts:156,162 e IdLawyer.tsx:128. CORRECCION: IdLawyer.tsx:261 'Unassigned' es label del ActionType 'unassign' (ACTION_META, no status) — no aplica al dedup de status labels.

### C2 — capa PRODUCTO · esfuerzo FE bajo · confianza high
- **Causa raiz:** Existen dos rutas de rechazo tecnicamente distintas y funcionales (TRASHED recuperable con purga 30d + restore, vs LOST terminal), ambas ya cableadas en front y back. La UI las presenta juntas en el footer del modal de un solo lead sin declarar el destino, causando ambiguedad. No hay defecto de contrato backend ni campo faltante: el fix es copy/diferenciacion 100% frontend (bajo esfuerzo). Lo que realmente bloquea la resolucion completa es una decision de PRODUCTO (redundancia Trash vs Lost), no trabajo backend — por eso reclasifico de FE+BE a PRODUCTO manteniendo isFrontend=true (hay una mejora de copy shippable hoy).
- **Frontend (fix aqui):** Confirmado FE y solo-FE: en LeadInfoModal.tsx:942-948 el boton "Move to Trash" (onTrash -> papelera recuperable) convive en el mismo footer con el boton submit que muestra "Mark as Lost" cuando isDestructive (LeadInfoModal.tsx:957-973), sin copy que declare que van a estados distintos. El warning "This action is permanent... Once marked as lost..." (LeadInfoModal.tsx:751-757) solo cubre la ruta LOST. Trabajo FE accionable HOY, sin tocar contrato backend: (1) agregar helper/subtitulo bajo "Move to Trash" declarando papelera RECUPERABLE con auto-purga a los TRASH_PURGE_DAYS (30) dias y restaurable — la constante ya esta importada y ya se usa para calcular purgeDate en LeadInfoModal.tsx:29 y :456; (2) diferenciar visual/textualmente ambos botones (tooltip/leyenda) Trash=recuperable vs Lost=cierre terminal; (3) reusar el copy del bulk dialog de trash (LeadManagement.tsx:1305-1315, ya dice "auto-purged after 30 days" y "can be restored") como fuente unica. Todos los strings viven en estos componentes front, describen comportamiento YA implementado, no requieren campo nuevo del backend.
- **Necesita backend:** Ninguno para el fix. Las dos rutas YA existen y difieren en backend, y el frontend ya las consume: "Move to Trash" -> api.leads.trash (LeadManagement.tsx:427-439) estado TRASHED recuperable con restore (handleRestore LeadManagement.tsx:412-425) y purga; "Mark as Lost" -> api.leads.unassign/update con status LOST/SEND_BACK terminal (LeadManagement.tsx:358-367). No hace falta ningun campo, payload, error_code ni scoping nuevo para la copy. Backend SOLO se toca condicionalmente y aguas abajo, si Producto decide que una ruta es redundante: ahi habria que eliminar endpoint/estado sobrante y migrar historico (enum en BD, scoping de queries). Eso es trabajo especulativo, no comprometido hoy — por eso la clasificacion original FE+BE sobre-declara backend.
- **Decision de producto:** Pregunta abierta #3: definir si "Move to Trash" (TRASHED recuperable, 30d) y "Mark as Lost" (LOST terminal enviado a super admin) deben coexistir o si una es redundante. Es el gate real del item: la copy FE se puede escribir hoy porque describe el comportamiento ya implementado, pero la resolucion de fondo (consolidar/eliminar una ruta) depende de esta decision de dominio, no de ingenieria backend. Solo si Producto decide consolidar se dispara el trabajo BE de migracion/enum.
- **Evidencia:** LeadInfoModal.tsx:942-948 (boton Move to Trash -> onTrash); LeadInfoModal.tsx:957-973 (submit muestra "Mark as Lost" solo si isDestructive); LeadInfoModal.tsx:751-757 (warning "permanent" solo LOST); LeadInfoModal.tsx:29 y :456 (TRASH_PURGE_DAYS importado y ya usado para purgeDate); LeadInfoModal.tsx:932-935 (footer ya distingue "In trash — will be auto-purged" vs "sent to super admin review"); LeadManagement.tsx:427-439 (handleTrash -> api.leads.trash recuperable); LeadManagement.tsx:412-425 (handleRestore); LeadManagement.tsx:358-367 (LOST/SEND_BACK -> unassign/update terminal); LeadManagement.tsx:1305-1315 (bulk trash dialog: "auto-purged after 30 days" + "can be restored"); leadStatusMeta.ts:153 (TRASH_PURGE_DAYS=30, constante FE); leadStatusMeta.ts:162-165 (DESTRUCTIVE=LOST,SEND_BACK); status.ts:19-22 ("Send back"->LOST). Todos los file:line citados existen y sostienen la causa raiz.

### C3 — capa FE+BE · esfuerzo FE bajo · confianza high
- **Causa raiz:** El copy del banner afirma un contrato de negocio (estado terminal, no reingreso a cola activa, revision de super admin) que el propio frontend contradice al ofrecer la transicion LOST->NEW en un option-set compartido con DISABLED. La coherencia copy-vs-affordance y el label del estado son FE puro; la veracidad de "terminal" (guard de transicion) es backend; y si LOST debe ser terminal o reversible es decision de producto. La "revision por super admin" resulta plausiblemente real (rol y audit_log documentados en FE), por lo que ese fragmento del copy NO se confirma como bug.
- **Frontend (fix aqui):** SOSTENIDA con matices. Verificado que hay trabajo FE real y accionable HOY sin cambio de payload backend (los status options son constantes definidas 100% en el cliente, no dependen de ningun campo del servidor). (1) [FE solido, gate de PRODUCTO] Contradiccion copy-vs-affordance CONFIRMADA: banner LeadInfoModal.tsx:752-757 promete "will not be reinstated to the active queue", pero un lead LOST recibe STATUS_OPTIONS_DISABLED (LeadManagement.tsx:1004-1006) que incluye { name:'New', value:'NEW' } (linea 105) = reingreso a cola activa. OJO para el fix: ese set esta COMPARTIDO entre DISABLED y LOST (linea 1004-1005), y para un lead DISABLED reactivar a NEW SI es legitimo; por tanto el fix FE correcto es SEPARAR un option-set propio para LOST (no borrar NEW del set compartido, que romperia DISABLED). Sigue siendo edicion pura FE. (2) [FE solido, sin gate] Inconsistencia de copy interna: el banner dice "Once marked as lost" pero el label real del estado LOST es "Sent back" (leadStatusMeta.ts:80) y el boton/opcion se llama "Send back" (LeadManagement.tsx:86,99,106). Alinear el texto del banner a "sent back" es FE puro, sin decision de producto ni backend. (3) [DEGRADADO — ya NO es un fix claro] La afirmacion original de que "no existe review de super admin, borrar el copy" se debilita: el rol 'super admin' SI existe en FE (useNotifications.ts:36) y el propio FE documenta el mecanismo (FaqDialog.tsx:34: el reason se guarda en audit_log.comment y "shown to super admin in the lead's history"; tambien AllLeads.tsx:511 "Super admin will see this comment"). O sea la revision por super admin es plausiblemente real (via history/audit), no aspiracional; NO recomendar eliminar ese copy hasta que backend lo desmienta.
- **Necesita backend:** Verificar en backend staging (fuera de este repo): (a) que la transicion LOST/SEND_BACK sea terminal — que ningun endpoint de status permita revertir LOST->NEW o exista guard de transicion — dado que el FE SI expone esa opcion (LeadManagement.tsx:105); si el backend la rechaza, el FE muestra un affordance roto (opcion que erroraria) y basta el fix FE; si la acepta, el copy de permanencia es falso. (b) confirmar la naturaleza real de la "revision por super admin": el FE asume que super admin ve el comentario en el lead history/audit_log (FaqDialog.tsx:34, AllLeads.tsx:511), no una cola dedicada; validar si eso satisface el copy "will be reviewed by the super admin" o si se prometio un workflow de review-queue que no existe. Ninguna de estas verificaciones es resoluble desde el cliente (onSubmit solo envia status/comments/doNotContact — LeadInfoModal.tsx:263-267 — sin logica de terminalidad).
- **Decision de producto:** Definir con el cliente si "Send back / LOST" es TERMINAL (entonces el FE debe dejar de ofrecer NEW para leads LOST — separando el option-set — y el copy de permanencia es correcto) o REVERSIBLE (entonces el copy "This action is permanent / will not be reinstated" debe ajustarse). Esta decision de dominio es la que gatilla cual de las dos ediciones FE se aplica en el punto (1); el fix (2) de alinear "marked as lost"->"sent back" no depende de esta decision.
- **Evidencia:** LeadInfoModal.tsx:752-757 (copy permanencia, verificado literal); LeadInfoModal.tsx:935 (footer "Logged & sent to super admin review", verificado); LeadInfoModal.tsx:263-267 (onSubmit solo status/comments/doNotContact, sin terminalidad, verificado); leadStatusMeta.ts:161-165 (DESTRUCTIVE_STATUSES={LOST,SEND_BACK}, verificado); leadStatusMeta.ts:79-80 (LOST label 'Sent back', verificado); LeadManagement.tsx:82-109 (STATUS_OPTIONS_DISABLED incluye NEW en linea 105 y es set COMPARTIDO; 'Send back'/'Send Back' en 86,99,106, verificado); LeadManagement.tsx:1004-1006 (DISABLED||LOST usan STATUS_OPTIONS_DISABLED, verificado). Matiz nuevo hallado: super admin es rol real (useNotifications.ts:36) y su 'review' via history/audit esta documentado en FaqDialog.tsx:34 y AllLeads.tsx:511 — degrada la recomendacion de borrar ese copy. routes.ts usa solo rol:['admin'] sin ruta de review-queue dedicada.

### D1 — capa FE+BE · esfuerzo FE medio · confianza high
- **Causa raiz:** El click de notificacion descarta el identificador del recurso: handleTrueNotification marca leida y navega a la raiz del listado por rol, sin leer entity_id. No hay mecanismo de preseleccion (sin useSearchParams). Ademas el texto se recorta con `truncate` sin vista de contenido completo (esto ultimo es puramente FE).
- **Frontend (fix aqui):** Confirmado tras leer los archivos. (1) BACKEND-INDEPENDIENTE, arreglable HOY solo en FE: src/components/Layout/Header.tsx:190-195 envuelve res.text en clase `truncate` (recorte a 1 linea) con solo tooltip nativo `title` — de ahi el reporte "no se ve completa". Quitar `truncate` (permitir wrap) o anadir expansion in-panel. Esta parte NO depende del backend y justifica por si sola isFrontend=true. (2) GATED por backend: handleTrueNotification en src/components/Layout/Header.tsx:76-81 y src/components/Layout/HeaderMobile.tsx:39-43 hace router.push fijo a '/lead-management' (admin) o '/select-lead' (lawyer), ignorando el recurso. El handler puede leer res.entity_id (el tipo laxo NotificationItem propaga la key en runtime, useNotifications.ts:12 `[k:string]:any`) y navegar al lead concreto — pero solo queda FUNCIONAL si el backend rellena entity_id. (3) Deep-link en el listado: src/app/(dashboard)/lead-management/LeadManagement.tsx NO tiene useSearchParams (verificado por grep, sin match); selectedLead solo se setea por click de fila (setSelectedLead :278, estado inicial :152, modal render :976+). Anadir lectura de ?leadId= es FE-doable y existe GET /leads/:id (usado en :469) para traer un lead fuera de pagina, pero paginacion/scoping complican la preseleccion. Items 2 y 3 solo funcionan si el backend persiste entity_id.
- **Necesita backend:** Confirmar/garantizar que el emisor de notificaciones rellena entity_type='lead' y entity_id en el payload real de staging. El DTO ya lo declara (src/types/api.types.ts:418-419) pero el DTO no prueba que el backend lo emita; si llega null, el deep-link y la navegacion al recurso concreto son imposibles desde FE. Backend es staging remoto (no en este repo), no verificable aqui.
- **Decision de producto:** Definir la superficie de destino del deep-link: no existe ruta /leads/:id de detalle (solo el listado /lead-management + LeadInfoModal por click). Ademas el destino difiere por rol: admin va a /lead-management, lawyer va a /select-lead — hay que decidir donde aterriza la notificacion de cada rol y si se abre modal con lead preseleccionado.
- **Evidencia:** src/components/Layout/Header.tsx:76-81 (push fijo /lead-management o /select-lead); Header.tsx:190-195 (truncate + solo title, FE puro); src/components/Layout/HeaderMobile.tsx:39-43 y :82,:94 (mismo handler generico); src/types/api.types.ts:410-429 (NotificationDTO con entity_type/entity_id en 418-419); src/hooks/useNotifications.ts:7-13 (NotificationItem laxo, `[k:string]:any` en :12 propaga entity_id en runtime); src/app/(dashboard)/lead-management/LeadManagement.tsx: grep confirma SIN useSearchParams; selectedLead solo por click de fila (:152 estado, :278 setSelectedLead, modal :976+); :469 usa GET /leads/:id

### D3 — capa FE+BE · esfuerzo FE bajo · confianza med
- **Causa raiz:** el DTO no tiene campo de lectura; la FE repurposa el ambiguo `is_active` como "no leído" y lo PUT-ea a ciegas. El "50" es el page-size por defecto del backend en `/notifications` → el contador está acotado por esa página, no refleja el unread real global.
- **Frontend (fix aqui):** mark-read individual + bulk + contador-desde-unread YA están implementados (`useNotifications.ts:61-104`; `Header.tsx:55,77,84`; `HeaderMobile.tsx:19,40`). Único gap FE-only pendiente: el punto "no leído" usa un campo inexistente `res.seen` (`Header.tsx:183`, `HeaderMobile.tsx:90`) → siempre muestra el dot; leer el campo de lectura real. Cleanup menor.
- **Necesita backend:** no existe estado-de-lectura por destinatario. `NotificationDTO` (`api.types.ts:410-429`) solo tiene `is_active` (flag activo/soft-delete, ambiguo) y `status` (estado de envío), sin `is_read`/`read_at`. Falta: (1) campo aditivo `is_read`/`read_at`; (2) endpoints mark-read individual y bulk; (3) endpoint unread-count desacoplado del page de 50; y confirmar si `is_active` es whitelisted como estado de lectura.
- **Decision de producto:** qué significa "leído" para admin (lista global compartida) vs lawyer (per-destinatario); si el badge debe mostrar el unread total más allá del cap de 50 ("50+").
- **Evidencia:** `api.types.ts:410-429` (sin is_read/read_at); `useNotifications.ts:61-104` (is_active reinterpretado como read-flag); `database.ts:1241-1296` (sin endpoint de lectura).

### E1 — capa FE · esfuerzo FE bajo · confianza high
- **Causa raiz:** No es bug de datos ni de backend. Spam Settings es un catalogo de REGLAS (blacklist + patrones regex); los leads flagged no se persisten como reglas, por eso 'Blacklist 0 / Suspicious Patterns 0' es el estado vacio CORRECTO. Los leads flagged YA tienen (a) lista dedicada: tab Review via GET /leads/review, y (b) detalle: spam_score+spam_reasons en LeadInfoModal. El unico gap del reporte es discoverability/copy en la pantalla equivocada — 100% frontend. El analista previo sobre-clasifico a FE+BE al no ver el endpoint /leads/review ya existente.
- **Frontend (fix aqui):** Arreglo FE puro, bajo esfuerzo y sin cambio de contrato backend: en src/app/(dashboard)/spam-settings/SpamSettings.tsx:462 el componente PageHead YA soporta la prop `subtitle` (verificado en PageHead.tsx:8, renderiza <p> en linea 37-41). Anadir copy/subtitle que aclare que esta pantalla es el CATALOGO DE REGLAS (Blacklist + Suspicious Patterns) y NO el registro de detecciones, y enlazar a Lead Management > Review, que YA es la lista dedicada de leads flagged. CORRECCION al analista previo: la lista dedicada de 'leads marcados como spam' NO esta bloqueada por backend — ya existe GET /leads/review (database.ts:1053, api.leads.review devuelve Paginated<LeadDTO>) que alimenta el tab Review (LeadManagement.tsx:1111-1115), y el detalle (spam_score 0-3 + spam_reasons) ya se renderiza en LeadInfoModal.tsx:417-439. Todo el fix del reporte (matar la confusion del '0/0') se resuelve en frontend con copy + link, usando datos y endpoints que ya existen.
- **Necesita backend:** ninguno requerido para resolver el reporte. La lista de leads flagged YA existe via GET /leads/review (database.ts:1053) y el detalle via spam_score/spam_reasons en LeadDTO (src/types/api.types.ts:113-114). El endpoint /spam/detections que proponia el analista previo es una MEJORA opcional de auditoria, no un bloqueante: el namespace spam (database.ts:1204-1236) solo tiene blacklist+patterns, pero no hace falta tocarlo. Anadir filtro spam/flagged a LeadFilters (api.types.ts:119-129, hoy sin ese parametro) solo seria necesario si se quisiera filtrar spam dentro de otros tabs — innecesario porque /leads/review ya segmenta los flagged.
- **Decision de producto:** Opcional y NO bloqueante para el fix de copy: decidir si Spam Settings debe incorporar una pestana 'Detecciones/Audit' propia o si esa auditoria permanece en Lead Management > Review (donde ya vive). La pregunta abierta #4 (¿marcar spam alimenta auto la blacklist o requiere confirmacion manual? — hoy markSpam solo mueve a Trash, LeadManagement.tsx:397-409) afecta solo a la mejora futura de auditoria, no al arreglo de discoverability.
- **Evidencia:** src/app/(dashboard)/spam-settings/SpamSettings.tsx:154,166,462 (solo blacklist.list+patterns.list; PageHead sin subtitle); src/components/ui/molecules/PageHead/PageHead.tsx:8,37-41 (prop subtitle ya soportada → fix FE trivial); src/services/database.ts:1053-1058 (api.leads.review → GET /leads/review devuelve Paginated<LeadDTO>=LISTA de flagged, contradice 'no hay endpoint'); database.ts:1204-1236 (namespace spam solo blacklist+patterns); src/components/ui/organisms/LeadInfoModal/LeadInfoModal.tsx:417-439 (spam_score/3 + spam_reasons ya renderizados); src/app/(dashboard)/lead-management/LeadManagement.tsx:397-409 (markSpam a Trash) y 1111-1115 (tab Review); src/types/api.types.ts:113-114 (spam_score/spam_reasons existen) y 119-129 (LeadFilters sin parametro spam/flagged, no necesario). Nota: analista previo cito 'api.types.ts' bajo src/services; ruta real es src/types/api.types.ts (mismas lineas).

### G1 — capa FE · esfuerzo FE bajo · confianza high
- **Causa raiz:** La tarjeta Pipeline Overview comparte un grid de 3 columnas con dos KpiCards; en la columna estrecha resultante el layout interno del PipelineChart (dona SVG de tamano fijo 132px con flex-shrink-0 + gap-8 + leyenda sin wrap/min-width) excede el ancho disponible y se trunca. Puro layout/presentacion cliente sin dependencia de backend.
- **Frontend (fix aqui):** Corregir la maquetacion responsive de la tarjeta Pipeline Overview en el dashboard del rol abogado. En src/app/(dashboard)/dash-lawyers/DashboardLawyers.tsx:262 la fila 2 es un grid `sm:grid-cols-2 lg:grid-cols-3` donde <PipelineChart> (linea :278) es el 3er hijo junto a dos KpiCards (Waiting/Flagged), por lo que en lg cae en una columna de ~1/3 de ancho. Dentro de src/components/ui/molecules/PipelineChart/PipelineChart.tsx la dona SVG tiene tamano fijo SIZE=132 (RADIUS 52 + STROKE 14, lineas 18-20) y esta marcada `flex-shrink-0` (linea 57) dentro de un contenedor `flex items-center gap-8` (lineas 50-55); la leyenda es `flex flex-col gap-2.5` sin `flex-wrap` ni `min-w-0` (linea 103), y sus filas tampoco truncan. 132 + gap 32 + leyenda excede el ancho de la celda y se corta en el borde derecho. Fix 100% FE: (a) sacar la tarjeta Pipeline del grid compartido de 3 col con los KPIs o darle su propia fila / `col-span`, y (b) volver fluido el layout interno de PipelineChart: `flex-wrap`, reducir gap en breakpoints chicos y permitir que la dona reduzca su SIZE en pantallas estrechas en lugar del 132px fijo. Verificar 390/768/1024/1440. Los datos (pipelineSegments) se derivan en cliente desde `leads` (DashboardLawyers.tsx:197-226) y no dependen de la forma del payload.
- **Evidencia:** src/app/(dashboard)/dash-lawyers/DashboardLawyers.tsx:262 (grid sm:grid-cols-2 lg:grid-cols-3), :278 (<PipelineChart> como 3er hijo), :197-226 (pipelineSegments derivados en cliente desde leads); src/components/ui/molecules/PipelineChart/PipelineChart.tsx:18-20 (RADIUS 52/STROKE 14/SIZE=(52+14)*2=132 fijo), :50-55 (flex items-center gap-8), :57 (dona flex-shrink-0), :103 (leyenda flex flex-col gap-2.5 sin wrap/min-w). Todos los file:line citados existen y sostienen la causa raiz.

---

## NO es frontend (solo backend)

- **D2** — El backend inserta N notificaciones (4) para un solo evento de spam-flag sobre un unico lead, sin clave de idempotencia (evento, recurso, destinatario). Probable disparo por regla-por-patron, por reintento de job, o por trigger duplicado. El FE es un consumidor pasivo: hace GET y muestra cada fila tal cual. _(BE: El emisor de notificaciones en el backend genera 4 registros para un unico evento de flag de spam sobre un solo lead. Corregir en el servicio/job de notificaciones: (1) idempotencia por clave (evento, recurso/lead_id, destinatario) antes de INSERT; (2) revisar si el trigger se dispara una vez por regla/patron coincidente o por reintentos del job de Bull, y agregar los motivos en una sola notificacion; (3) constraint unica en la tabla de notificaciones para el mismo (tipo, lead_id, lawyer_id, ventana de tiempo). Endpoints implicados (solo lectura desde FE): GET /notifications, GET /notifications/lawyer/:id, GET /notifications/history/all. La emision no existe en este repo (backend staging remoto).. Evidencia: src/hooks/useNotifications.ts:33-70 (fetch + parse/filter/sort, sin dedup ni emision); src/services/database.ts:1276-1281 (notifications.history -> /notifications/history/all, solo GET); grep confirma que ni spam-settings ni LeadInfoModal ni ningun flujo FE emite notificaciones al crear/flagear un lead.)_

## Bloqueado por backend (FE no accionable hoy)

- **F1** — capa FE+BE · esfuerzo FE alto · la sección `Firms` solo permite *Merge* por ID numérico manual; faltan listar/crear/editar firmas y el selector de catálogo. **El trabajo FE está bloqueado:** `database.ts` (namespace `firms.*`, ~1315-1363) NO expone GET de catálogo ni create/edit de firmas — solo `me/listLawyers/addLawyer/setAdmin/updateSettings/leads/merge`. Hasta que el backend exponga esos endpoints no se puede construir el listado/selector ni resolver B4. Además `Merge` por ID escrito a mano es irreversible sin confirmación por nombre. Decisión de producto: ¿quién crea firmas (super admin / firm admin / ambos)?

## Bloqueado por decision de producto

- **C2** — Pregunta abierta #3: definir si "Move to Trash" (TRASHED recuperable, 30d) y "Mark as Lost" (LOST terminal enviado a super admin) deben coexistir o si una es redundante. Es el gate real del item: la copy FE se puede escribir hoy porque describe el comportamiento ya implementado, pero la resolucion de fondo (consolidar/eliminar una ruta) depende de esta decision de dominio, no de ingenieria backend. Solo si Producto decide consolidar se dispara el trabajo BE de migracion/enum.
